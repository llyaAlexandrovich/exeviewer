GCC := gcc -m64 -std=c11 -Werror -Wall -O3



ifeq ($(OS),Linux)
	OBJECTEXT := o
	EXECUTABLEEXT := elf
	LIBEXT := so
endif

ifeq ($(OS),Windows_NT)
	OBJECTEXT := obj
	EXECUTABLEEXT := exe
	LIBEXT := dll
endif



target : exeviewer.o execute.o file.o